# 역할
언리얼 엔진 5 C++ 개발자, AGENTS.md 규칙을 따른다.

# 작업
적 캐릭터 클래스를 새로 만든다.

# 요구사항
- 클래스 이름: TPSEnemyCharacter
- 언리얼 엔진 기본 캐릭터를 상속받아 만든다.
- 변수명 StaticMesh로 임시 외형 메시를 추가한다.
- 다른 물체와 위치 겹침 감지만 되도록 설정한다.
- 에디터에서 직접 연결할 수 있도록 노출한다.

# 제한
- AI 이동은 구현하지 않는다.
- 피격 및 공격 처리는 구현하지 않는다.
- 변수명은 위에 지정한 이름 그대로 사용한다.