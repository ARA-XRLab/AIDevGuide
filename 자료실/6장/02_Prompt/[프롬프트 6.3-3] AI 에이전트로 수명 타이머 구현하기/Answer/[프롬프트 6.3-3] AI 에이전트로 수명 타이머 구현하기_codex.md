• Edited Source\SpaceShooter\Private\EnemyBulletActor.cpp (+12 -8)
    81  
    82 -        if (bActive)
    83 -        {
    84 -        }
    85 -        else
    86 -        {
    87 -                GetWorldTimerManager().ClearTimer(LifeTimeTimerHandle);
    88 -        }
    89 -}
    82 +        if (bActive)
    83 +        {
    84 +                if (LifeTime > 0.f)
    85 +                {
    86 +                        GetWorldTimerManager().SetTimer(LifeTimeTimerHandle, this, &AEnemyBulletActor::OnLifeTimeExpired, LifeTime, false);
    87 +                }
    88 +        }
    89 +        else
    90 +        {
    91 +                GetWorldTimerManager().ClearTimer(LifeTimeTimerHandle);
    92 +        }
    93 +}
    94  

─ Worked for 6s ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• SetActive(true) 때 LifeTime 타이머 시작하도록 추가했어요. Source/SpaceShooter/Private/EnemyBulletActor.cpp

  다음 단계로 원하면:

  1. 타이머 만료 시 Destroy/풀 반환까지 이어지게 개선
  2. EnemyBullet 충돌 프로필/DestroyZone 오버랩 확인