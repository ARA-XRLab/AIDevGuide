✦ 분석 결과, BP_EnemyBullet_C가 계속 증가하는 주된 이유는 플레이어와 충돌했을 때 Actor가 파괴되지 않고 비활성화만 되기 때문입니다.


  원인 분석
   1. 플레이어 충돌 시 로직: EnemyBulletActor.cpp의 OnBoxCompOverlap 함수에서 플레이어와 충돌하면 SetActive(false)를 호출합니다.
   2. SetActive(false)의 동작: 제가 수정해드린 코드에 따르면 SetActive(false) 호출 시 LifeTimeTimerHandle이 해제(Clear)됩니다. 또한 메쉬가 숨겨지고 충돌이 꺼지지만, Actor 자체는 월드에
      남아 있습니다.
   3. 풀링(Pooling) 미사용: 플레이어 총알과 달리 적 총알(AEnemyActor::FireOneBullet)은 매번 SpawnActor로 생성됩니다. 따라서 충돌 후 파괴(Destroy)되지 않고 SetActive(false) 상태로 남은
      총알들이 메모리에 계속 누적되는 것입니다.
   4. 타이머 신뢰성: OnLifeTimeExpired가 UFUNCTION()으로 선언되어 있지 않아 일부 환경에서 타이머가 정상적으로 작동하지 않았을 가능성도 있습니다.


  해결 방법
  현재 구조에서 적 총알은 별도의 풀링을 사용하지 않으므로, 플레이어와 충돌 시 SetActive(false) 대신 Destroy()를 호출해야 합니다. 또한 타이머 함수를 UFUNCTION()으로 등록하여 안정성을
  높입니다.


  1. 헤더 수정 (EnemyBulletActor.h)
  OnLifeTimeExpired를 UFUNCTION()으로 선언합니다.

   1 // Source/SpaceShooter/Public/EnemyBulletActor.h
   2
   3 protected:
   4     // ... 기존 코드 ...
   5     UFUNCTION() // 추가
   6     void OnLifeTimeExpired();


  2. 소스 수정 (EnemyBulletActor.cpp)
  충돌 시 Destroy()를 호출하고 로그를 추가하여 확인합니다.


    1 // Source/SpaceShooter/Private/EnemyBulletActor.cpp
    2
    3 void AEnemyBulletActor::OnBoxCompOverlap(
    4     UPrimitiveComponent* OverlappedComponent, AActor* OtherActor,
    5     UPrimitiveComponent* OtherComp, int32 OtherBodyIndex, bool bFromSweep,
    6     const FHitResult& SweepResult)
    7 {
    8     APlayerPawn* Player = Cast<APlayerPawn>(OtherActor);
    9     if (Player)
   10     {
   11         if (!Player->IsInvincible())
   12         {
   13             Player->OnDamage(Damage);
   14         }
   15
   16         // SetActive(false) 대신 바로 Destroy 호출 (풀링을 안 하므로)
   17         Destroy(); 
   18     }
   19 }


  이 수정을 진행할까요? 직접 반영해 드릴 수 있습니다.